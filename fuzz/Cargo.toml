[package]
name = "ryu-fuzz"
version = "0.0.0"
publish = false
edition = "2018"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = { version = "0.4", features = ["arbitrary-derive"] }
ryu = { path = ".." }

[[bin]]
name = "fuzz_ryu"
path = "fuzz_targets/fuzz_ryu.rs"
test = false
doc = false

[workspace]
